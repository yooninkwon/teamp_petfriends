<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.tech.petfriends.community.mapper.CDao"> 
    <select id="postlist" resultType="com.tech.petfriends.community.dto.CDto">
        SELECT * FROM COMMUNITY_BOARD
        ORDER BY BOARD_NO DESC
    </select>
</mapper>