<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.tech.petfriends.admin.mapper.AdminPageDao"> 
	<select id="getPetteacherList" resultType="com.tech.petfriends.helppetf.dto.PetteacherDto">
		SELECT * FROM HELP_PETTEACHER
		<where>
			<choose>
				<when test="type == 'all'">HPT_HIT >= 0</when>
				<when test="type == 'cat'">HPT_PETTYPE = 'cat'</when>
				<when test="type == 'dog'">HPT_PETTYPE = 'dog'</when>
				<when test="type == 'etc'">HPT_PETTYPE = 'etc'</when>
			</choose>
			<choose>
				<when test="category == '0'"> </when>
				<when test="category == '1'">AND HPT_CATEGORY = '1'</when>
				<when test="category == '2'">AND HPT_CATEGORY = '2'</when>
				<when test="category == '3'">AND HPT_CATEGORY = '3'</when>
				<when test="category == '4'">AND HPT_CATEGORY = '4'</when>
				<when test="category == '5'">AND HPT_CATEGORY = '5'</when>
				<when test="category == '6'">AND HPT_CATEGORY = '6'</when>
			</choose>
		</where>
		<choose>
			<when test="sort == '최신순'">ORDER BY HPT_RGEDATE DESC</when>
			<when test="sort == '오래된순'">ORDER BY HPT_RGEDATE ASC</when>
			<when test="sort == '조회수순'">ORDER BY HPT_HIT DESC</when>
		</choose>
	</select>



	<!-- <select id="adminPetteacherList" resultType="com.tech.petfriends.admin.dto.AdminHelpPetfDto">
		SELECT * FROM HELP_PETTEACHER
		ORDER BY HPT_SEQ DESC
	</select>
	<select id="adminPetteacherDetail" resultType="com.tech.petfriends.admin.dto.AdminHelpPetfDto">
		SELECT * FROM HELP_PETTEACHER 
		WHERE HPT_SEQ = #{hpt_seq}
	</select>
	<insert id="adminPetteacherWrite">
		INSERT INTO HELP_PETTEACHER VALUES
		(SEQ_HPT.NEXTVAL, #{hpt_yt_videoid}, 
		#{hpt_exp}, #{hpt_title}, #{hpt_content}, 
		#{hpt_pettype}, #{hpt_category}, 0, SYSDATE)
	</insert>
	<delete id="adminPetteacherDelete">
		DELETE FROM HELP_PETTEACHER WHERE HPT_SEQ = #{hpt_seq}
	</delete> -->
</mapper>